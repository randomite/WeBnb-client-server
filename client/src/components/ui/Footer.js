import React from "react";
import { withRouter } from "react-router-dom";

class Footer extends React.Component {
  render() {
    return <footer style={{ position: "fixed" }}> </footer>;
  }
}

export default withRouter(Footer);
